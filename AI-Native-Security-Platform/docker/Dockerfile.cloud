FROM python:3.10-slim

WORKDIR /app

COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

COPY prototype /app/prototype

ENV PYTHONPATH=/app

CMD ["python", "-c", "from prototype.cloud.policy-engine.policy import CloudPolicyEngine; print('Cloud Policy Engine Ready.')"]
